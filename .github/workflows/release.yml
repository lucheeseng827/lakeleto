# lakeleto release pipeline — runs on the PUBLIC mirror (lucheeseng827/lakeleto).
#
# Synced from the private monorepo to `.github/workflows/release.yml` (see .ossync.yaml). Cuts a
# release on a `v*` tag. Lakeleto is a SINGLE crate (lakeleto → published as `lakeleto`; binary stays `lakeleto`), so this
# is simpler than a multi-crate workspace: one build, one `cargo publish`, no dep-order loop.
#   1. build   — static musl (x86_64 + aarch64) + macOS (x86_64 + arm64) + Windows x64 (.zip);
#                archive + sha256; cosign keyless signature (.sig/.pem) + SLSA provenance per artifact
#   2. sbom    — CycloneDX SBOM (cargo-cyclonedx) for the crate
#   3. release — attach every artifact + SHA256SUMS + signatures + SBOM to the GitHub release
#   4. crates  — `cargo publish` the single `lakeleto` crate (real tags only, idempotent)
#   5. brew    — rewrite Formula/lakeleto.rb with the tag version + the four desktop/linux sha256
#   6. docker  — buildx multi-arch (linux/amd64 + linux/arm64) distroless image → Docker Hub,
#                cosign-signed + SLSA-attested
#
# Every artifact + the image is signed (cosign keyless OIDC), carries SLSA provenance, and ships
# with a CycloneDX SBOM.
#
# Supply-chain hardening (matches the monorepo gates):
#   • every `uses:` is pinned to a full commit SHA with the human tag in a trailing comment —
#     a moved tag can't swap the action out from under a release. Refresh a pin with
#     `git ls-remote https://github.com/<action> refs/tags/<newtag>^{}` and update the comment.
#   • top-level `permissions: {}` (deny-all); each job elevates only the scopes it needs.
#   • every checkout runs `persist-credentials: false` so the job token is never written to
#     .git/config where a later build step could read it (zizmor "artipacked"); the brew job
#     re-supplies the token at the point of push only.
#
# Mirror repo secrets: DOCKER_USERNAME / DOCKER_PASSWORD (Docker Hub) and CRATESIO_TOKEN live in
# the `cicd` environment. cosign + SLSA use the workflow's OIDC token (no stored keys).

name: release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing tag to (re)build, e.g. v0.1.0"
        required: true
      promote:
        description: "Promote formula + :latest image (only safe for the newest tag)"
        type: boolean
        default: false

# Deny-all by default; every job below grants itself the minimum it needs.
permissions: {}

env:
  BIN: lakeleto
  IMAGE: docker.io/mancube/lakeleto
  # Server build profile: HTTP/JSON API + embedded SPA, SQL engine, Iceberg reader, object-store.
  FEATURES: serve,sql,iceberg,object-store
  TAG: ${{ github.event.inputs.tag || github.ref_name }}

jobs:
  validate:
    name: validate release tag
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false
      - name: Assert TAG is v<semver> and matches the crate version
        shell: bash
        run: |
          set -euo pipefail
          # A non-semver branch/ref, or a tag whose version disagrees with the crate
          # version, would let `cargo publish` (which uses the manifest version) push an
          # irreversible crates.io release while `wait_indexed` polls ${TAG#v} and then
          # times out. Fail closed BEFORE any build or publish runs.
          [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([-+][0-9A-Za-z.-]+)?$ ]] \
            || { echo "::error::TAG '$TAG' is not a v<semver> release tag"; exit 1; }
          ver="$(grep -m1 -E '^version[[:space:]]*=' Cargo.toml | sed -E 's/.*"([^"]+)".*/\1/')"
          # Compare the tag's BASE version (strip any -prerelease/+build suffix) to the crate
          # version, so an rc tag (v0.1.0-rc.1) can exercise the pipeline against the same
          # version. The crates.io + brew jobs still skip any tag containing '-', so only a
          # clean vX.Y.Z ever publishes — the "irreversible publish" guard is unchanged.
          base="${TAG#v}"; base="${base%%[-+]*}"
          [ "$base" = "$ver" ] \
            || { echo "::error::tag '$TAG' base version '$base' != crate version '$ver' (Cargo.toml)"; exit 1; }
          echo "validated: $TAG (release version $ver)"

  build:
    name: build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    needs: validate
    permissions:
      contents: read      # checkout the tagged source
      id-token: write     # cosign keyless + SLSA provenance (Sigstore OIDC)
      attestations: write # SLSA build-provenance attestation
    strategy:
      fail-fast: false
      matrix:
        include:
          - { target: x86_64-unknown-linux-musl,  os: ubuntu-latest, cross: true }
          - { target: aarch64-unknown-linux-musl, os: ubuntu-latest, cross: true }
          # Both macOS targets build on macos-14 (Apple Silicon): arm64 native, x86_64 via an
          # in-macOS cross-compile.
          - { target: x86_64-apple-darwin,         os: macos-14 }
          - { target: aarch64-apple-darwin,        os: macos-14 }
          # Windows x64: native MSVC build on windows-latest. Ships a `.zip` of lakeleto.exe
          # (the `archive` step below branches on the target); matches binstall's
          # `overrides.x86_64-pc-windows-msvc { pkg-fmt = "zip" }` in Cargo.toml.
          - { target: x86_64-pc-windows-msvc,      os: windows-latest }
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false

      - name: Install Rust
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross (linux musl)
        if: matrix.cross
        uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cross

      - name: Build (cross)
        if: matrix.cross
        run: cross build --release --bin "$BIN" --features "$FEATURES" --target ${{ matrix.target }}

      - name: Build (native)
        if: ${{ !matrix.cross }}
        run: cargo build --release --bin "$BIN" --features "$FEATURES" --target ${{ matrix.target }}
        shell: bash

      - name: Package
        id: pkg
        shell: bash
        run: |
          set -euo pipefail
          root="$PWD"
          dist="target/${{ matrix.target }}/release"
          case "${{ matrix.target }}" in
            *windows*)
              # Windows convention: a .zip of lakeleto.exe. 7z is preinstalled on windows-latest.
              bin="${BIN}.exe"
              asset="${BIN}-${{ matrix.target }}.zip"
              ( cd "$dist" && 7z a "$root/$asset" "$bin" >/dev/null )
              ;;
            *)
              bin="${BIN}"
              asset="${BIN}-${{ matrix.target }}.tar.gz"
              tar -czf "$asset" -C "$dist" "$bin"
              ;;
          esac
          if command -v sha256sum >/dev/null; then
            sha256sum "$asset" > "$asset.sha256"
          else
            shasum -a 256 "$asset" > "$asset.sha256"
          fi
          echo "asset=$asset" >> "$GITHUB_OUTPUT"

      - name: Sign artifact (cosign keyless)
        uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1
      - name: cosign sign-blob
        shell: bash
        run: |
          set -euo pipefail
          asset="${{ steps.pkg.outputs.asset }}"
          # Keyless: identity = this workflow's OIDC token; emits a detached sig + cert.
          cosign sign-blob --yes \
            --output-signature "$asset.sig" \
            --output-certificate "$asset.pem" \
            "$asset"

      - name: SLSA build provenance
        uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0
        with:
          subject-path: ${{ steps.pkg.outputs.asset }}

      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ${{ matrix.target }}
          path: |
            ${{ steps.pkg.outputs.asset }}
            ${{ steps.pkg.outputs.asset }}.sha256
            ${{ steps.pkg.outputs.asset }}.sig
            ${{ steps.pkg.outputs.asset }}.pem
          if-no-files-found: error

  sbom:
    name: cyclonedx sbom
    runs-on: ubuntu-latest
    needs: validate
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - uses: taiki-e/install-action@07b4745e0c39a41822af610387492e3e53aa222b # v2.83.4
        with:
          tool: cargo-cyclonedx
      - name: Generate SBOM (server feature set — the shipped binary's tree)
        shell: bash
        run: |
          set -euo pipefail
          cargo cyclonedx --format json --features "$FEATURES"
          # Collect the crate bom into one artifact directory.
          mkdir -p sbom
          find . -name '*.cdx.json' -not -path './sbom/*' -exec cp {} sbom/ \;
          ls -l sbom
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: sbom
          path: sbom/*.cdx.json
          if-no-files-found: error

  release:
    name: github release
    needs: [build, sbom]
    runs-on: ubuntu-latest
    permissions:
      contents: write # create the GitHub release + upload assets
    steps:
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with: { path: dist, merge-multiple: true }
      - name: Aggregate checksums
        # Block scalar (not a one-liner): concatenate the per-asset .sha256 sidecars.
        run: |
          ( cd dist && cat ./*.sha256 > SHA256SUMS && cat SHA256SUMS )
      - name: Publish release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2.6.2
        with:
          tag_name: ${{ env.TAG }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          # A pre-release suffix (vX.Y.Z-rc.N) is marked a GitHub pre-release — exercise the
          # pipeline without presenting an rc as a GA download.
          prerelease: ${{ contains(env.TAG, '-') }}
          files: |
            dist/*.tar.gz
            dist/*.zip
            dist/*.sha256
            dist/*.sig
            dist/*.pem
            dist/*.cdx.json
            dist/SHA256SUMS

  cratesio:
    name: publish lakeleto to crates.io
    needs: release
    # Publish only a real (non pre-release) tag that was pushed or explicitly promoted. crates.io
    # is append-only — never publish an rc.
    if: ${{ !contains(github.event.inputs.tag || github.ref_name, '-') && (github.event_name == 'push' || inputs.promote) }}
    environment: cicd
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout only; the publish authenticates with CRATESIO_TOKEN, not the job token
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable
      - name: Publish the lakeleto crate
        shell: bash
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATESIO_TOKEN }}
        run: |
          set -euo pipefail
          ver="${TAG#v}"
          crate="lakeleto"   # crates.io package name (binary stays `lakeleto`; `lakeleto` was squatted)
          # crates.io 403s a User-Agent-less curl, which would make the "already published?"
          # probe below misfire (treat a live release as absent, then error on re-publish). Send
          # an explicit UA on EVERY crates.io API call. (This bit mcpdef — keep the header.)
          UA="lakeleto-release (github.com/lucheeseng827/lakeleto)"
          # Idempotent: if this exact version is already indexed, this is a re-run — skip.
          if curl -sf -A "$UA" "https://crates.io/api/v1/crates/${crate}/${ver}" >/dev/null 2>&1; then
            echo "${crate} ${ver} already on crates.io — skipping"
            exit 0
          fi
          # Single crate — no dependency-order loop needed.
          cargo publish
          # Confirm the new version is queryable before the job exits (nice-to-have; a single
          # crate has no dependents waiting on it).
          for _ in $(seq 1 30); do
            if curl -sf -A "$UA" "https://crates.io/api/v1/crates/${crate}/${ver}" >/dev/null 2>&1; then
              echo "${crate} ${ver} indexed"; exit 0
            fi
            sleep 10
          done
          echo "::warning::timed out waiting for ${crate} ${ver} to index"

  brew:
    name: bump homebrew formula
    needs: release
    if: ${{ !contains(github.event.inputs.tag || github.ref_name, '-') && (github.event_name == 'push' || inputs.promote) }}
    runs-on: ubuntu-latest
    permissions:
      contents: write # commit + push the regenerated formula to main
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: main
          persist-credentials: false
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with: { path: dist, merge-multiple: true }
      - name: Generate Formula/lakeleto.rb
        shell: bash
        # Written fresh from the release's checksums. The mirror's Formula/lakeleto.rb is owned by
        # THIS job (not synced from the monorepo), so a sync can never clobber the real shas.
        run: |
          set -euo pipefail
          ver="${TAG#v}"
          sha() { cut -d' ' -f1 < "dist/${BIN}-$1.tar.gz.sha256"; }
          mac_arm=$(sha aarch64-apple-darwin)
          mac_x86=$(sha x86_64-apple-darwin)
          lin_arm=$(sha aarch64-unknown-linux-musl)
          lin_x86=$(sha x86_64-unknown-linux-musl)
          base="https://github.com/lucheeseng827/lakeleto/releases/download/v#{version}"
          mkdir -p Formula
          cat > Formula/lakeleto.rb <<EOF
          # Generated by .github/workflows/release.yml on each v* tag — do not hand-edit.
          class Lakeleto < Formula
            desc "Instant local lakehouse-table explorer (Parquet/CSV/Iceberg) in a single binary"
            homepage "https://github.com/lucheeseng827/lakeleto"
            version "$ver"
            license "Apache-2.0"

            on_macos do
              on_arm do
                url "$base/lakeleto-aarch64-apple-darwin.tar.gz"
                sha256 "$mac_arm"
              end
              on_intel do
                url "$base/lakeleto-x86_64-apple-darwin.tar.gz"
                sha256 "$mac_x86"
              end
            end

            on_linux do
              on_arm do
                url "$base/lakeleto-aarch64-unknown-linux-musl.tar.gz"
                sha256 "$lin_arm"
              end
              on_intel do
                url "$base/lakeleto-x86_64-unknown-linux-musl.tar.gz"
                sha256 "$lin_x86"
              end
            end

            def install
              bin.install "lakeleto"
            end

            test do
              assert_match "lakeleto", shell_output("#{bin}/lakeleto --help")
            end
          end
          EOF
          echo "--- generated formula ---"; cat Formula/lakeleto.rb
      - name: Commit
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/lakeleto.rb
          git commit -m "brew: lakeleto ${TAG}" || { echo "no formula change"; exit 0; }
          # Checkout ran with persist-credentials:false. Supply the token as an EPHEMERAL
          # per-command header — never written to .git/config or the remote URL. origin keeps
          # its tokenless https URL; the credential exists only for this one push.
          auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')"
          git -c "http.https://github.com/.extraheader=AUTHORIZATION: basic ${auth}" push origin HEAD:main

  docker:
    name: distroless image
    needs: release
    environment: cicd
    runs-on: ubuntu-latest
    permissions:
      contents: read      # checkout the tagged source (Dockerfile.release + DOCKERHUB.md)
      id-token: write     # cosign keyless + image SLSA provenance (Sigstore OIDC)
      attestations: write # SLSA provenance attestation for the image
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ env.TAG }}
          persist-credentials: false
      - uses: docker/setup-qemu-action@c7c53464625b32c7a7e944ae62b3e17d2b600130 # v3.7.0
      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0
      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: docker.io
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1
      - name: Stage prebuilt static binaries into the build context
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          path: art
          merge-multiple: true
      - name: Lay out the context
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p ctx
          tar -xzf art/lakeleto-x86_64-unknown-linux-musl.tar.gz  -C ctx && mv ctx/lakeleto ctx/lakeleto-amd64
          tar -xzf art/lakeleto-aarch64-unknown-linux-musl.tar.gz -C ctx && mv ctx/lakeleto ctx/lakeleto-arm64
          cp Dockerfile.release ctx/Dockerfile
      - name: Compute image tags
        id: imgtags
        env:
          PROMOTE: ${{ github.event_name == 'push' || inputs.promote }}
        shell: bash
        run: |
          tags="${IMAGE}:${TAG}"
          if [[ "$TAG" != *-* && "$PROMOTE" == "true" ]]; then tags="$tags"$'\n'"${IMAGE}:latest"; fi
          { echo "list<<EOF"; echo "$tags"; echo "EOF"; } >> "$GITHUB_OUTPUT"
      - name: Build + push
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          context: ctx
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.imgtags.outputs.list }}
      - name: cosign sign image (keyless)
        shell: bash
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: cosign sign --yes "${IMAGE}@${DIGEST}"
      - name: SLSA provenance for the image
        uses: actions/attest-build-provenance@e8998f949152b193b063cb0ec769d69d929409be # v2.4.0
        with:
          subject-name: ${{ env.IMAGE }}
          subject-digest: ${{ steps.build.outputs.digest }}
          push-to-registry: true
      - name: Update Docker Hub description
        if: ${{ !contains(env.TAG, '-') && (github.event_name == 'push' || inputs.promote) }}
        uses: peter-evans/dockerhub-description@432a30c9e07499fd01da9f8a49f0faf9e0ca5b77 # v4.0.2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: mancube/lakeleto
          readme-filepath: ./docs/DOCKERHUB.md
